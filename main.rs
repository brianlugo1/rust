// compile rust file with 'rustc main.rs'
// run executable with './main'

// fn = function
// main() is executed automatically when ran
fn main() {
    // Recommended to have four spaces and not tab
    // print line ! (! means macro)
    println!("hello world!");
}