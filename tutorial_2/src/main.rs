// cargo new "project_name" - creates a new rust project
// cargo build - compiles the rust project
// cargo run - compiles and automatically runs the compiled rust project
// cargo check - runs a test for bugs and compiles rust project without actually compiling
// cd src, run "rustfmt main.rs" to formate a rust file!
fn main() {
    println!("Hello, world!");
}
